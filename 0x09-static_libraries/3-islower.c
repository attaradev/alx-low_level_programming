#include "main.h"

/**
 * _islower - Entry point for programme
 *
 * @c: Character to check
 *
 * Description: Checks for lower case character and returns 1 if true else 0
 *
 * Return: Return 1
 */
int _islower(int c)
{
	return (c > 97 && c < 122);
}
